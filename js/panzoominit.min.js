/*global jQuery:false */

// Dependencies: bootstrap.js

(function ($) {
    "use strict";
	
    if (typeof ($.fn.carousel) !== 'undefined') {
        
        // Panzoom get next slide w/o waiting
        $('.carousel.panzoom').carousel('next');

    }

}(jQuery));